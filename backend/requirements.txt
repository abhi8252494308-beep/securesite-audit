# Core framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Database
sqlalchemy==2.0.36
aiosqlite==0.20.0
alembic==1.14.0
asyncpg==0.29.0  # PostgreSQL driver for production

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
cryptography==42.0.8  # Pre-built wheels available, no Rust needed
bcrypt==4.1.2

# Data validation - pin compatible versions with prebuilt wheels
pydantic==2.7.4
pydantic-core==2.18.4
pydantic-settings==2.5.2
python-dotenv==1.0.1

# HTTP client
httpx==0.28.1

# DNS resolution
dnspython==2.7.0

# PDF generation - use reportlab instead of weasyprint (no system deps)
# weasyprint==60.1  # Requires Cairo/Pango/GDK-PixBuf system libraries
reportlab==4.2.2
jinja2==3.1.4

# Utilities
validators==0.34.0
user-agents==2.2.0

# MongoDB
motor==3.4.0
pymongo==4.8.0

# Redis (optional)
redis==5.0.8